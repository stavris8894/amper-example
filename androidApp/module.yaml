product: android/app

dependencies:
  - ../shared
  - $libs.androidx.compose.ui.tooling.preview
  - $libs.androidx.activity.compose
  - $compose.foundation

settings:
  compose: enabled
  android:
    applicationId: com.example.amper
    namespace: com.example.amper
